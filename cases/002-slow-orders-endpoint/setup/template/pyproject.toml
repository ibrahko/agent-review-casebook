[project]
name = "shop-api"
version = "0.1.0"
description = "Tiny order API used as the starting point of a Casebook task."
requires-python = ">=3.12"
dependencies = [
    "django>=5.1,<6",
    "djangorestframework>=3.15",
]

[tool.uv]
package = false
